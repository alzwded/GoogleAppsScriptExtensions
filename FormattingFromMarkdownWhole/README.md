While I know you can render Markdown to some Rich Text variant and paste that into Gdocs and it will look right...

...why not write a GAS?

This goes line-by-line and converts rather vanilla markdown to properly formatted text.

I've tested it with an 18 page spec (which became 20 pages) which looks stellar.

I haven't tested tables at time of writing, so who knows.
